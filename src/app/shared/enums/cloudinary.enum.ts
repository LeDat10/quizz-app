export enum ResourceType {
  IMAGE = 'image',
  RAW = 'raw',
  VIDEO = 'video',
  AUTO = 'auto',
}

export enum StatusType {
  DRAFT = 'draft',
  PUBLISHED = 'published',
  INACTIVE = 'inactive',
  ARCHIVED = 'archived',
}
